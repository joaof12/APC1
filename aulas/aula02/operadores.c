#include <stdio.h>

int main () {



  int multiplicação = 2 * 2;
  int divisao = 10 / 2;
  int diferença = 3 - 2;
  int soma = 5 + 5;

  printf("soma = %i\n", soma);
  printf("soma = %i\n", ++soma); // soma = soma + 1
  printf("soma = %i\n", soma);
  printf("soma = %i\n", soma++); // soma = soma - 1 
  
  
  return 0;
}